{
  "docs": [
    {
      "_id": "_design/metadata",
      "views": {
        "combined": {
          "map": "function (doc) { if (doc.type === \"metadata\") { emit([doc.uid, doc.source, doc.image_id], doc); } }"
        }
      }
    }
  ]
}